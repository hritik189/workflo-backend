apiVersion: v1
kind: Service
metadata:
  name: {{ include "workflo-api.fullname" . }}
  labels:
    {{- include "workflo-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "workflo-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
